## Overview
#
# This reusable GitHub Actions workflow provides a comprehensive Continuous Integration (CI) pipeline
# for multi-platform mobile and desktop applications, specifically designed for projects using Gradle and Java/Kotlin.
#
### Key Features
# - Automated code quality checks
# - Dependency management and verification
# - Cross-platform desktop application builds (Windows, Linux, MacOS)
# - Android APK build generation
# - Artifact generation and storage
#
### Workflow Jobs
# 1. **Setup**: Prepares the build environment
#   - Checks out repository code
#   - Sets up Java (default 17, configurable via `java-version` input)
#   - Configures Gradle
#   - Manages dependency caching
#
# 2. **Code Quality Checks**:
#   - Build logic verification
#   - Code formatting checks (Spotless)
#   - Static code analysis (Detekt)
#
# 3. **Dependency Guard**:
#   - Verifies dependencies against baseline
#   - Prevents unauthorized dependency changes
#   - Supports automatic baseline updates
#
# 4. **Android App Build**:
#   - Builds debug APK for demo flavor
#   - Uploads APK artifacts
#
# 5. **Desktop App Build**:
#   - Builds applications for Windows, Linux, and MacOS
#   - Generates platform-specific executables and packages
#
### Prerequisites
#   - Java (configurable, defaults to 17)
#   - Gradle
#   - Configured build scripts for:
#       - Android module
#       - Desktop module
#   - Installed Gradle plugins:
#       - Spotless
#       - Detekt
#       - Dependency Guard
#
### Configuration Parameters
# The workflow requires below parameters:
#
#   | Parameter | Description | Type | Required |
#   |-----------|-------------|------|----------|
#   | `android_package_name` | Name of the Android project module | String | Yes |
#   | `desktop_package_name` | Name of the Desktop project module | String | Yes |
#   | `web_package_name` | Name of the Web project module	 | String | Yes |
#   | `ios_package_name` | Name of the iOS project module	 | String | Yes |
#   | `build_ios` | Enable iOS build | Boolean | No |
#   | `use_cocoapods` | Whether to use CocoaPods for integrating the shared module into the iOS app | Boolean | Required **if `build_ios` = true** |
#   | `shared_module` | Gradle path to the shared Kotlin Multiplatform module used in the iOS app (e.g., :cmp-shared, :shared) | String | Required **if `build_ios` = true** |
#   | `java-version` | Java version to use (e.g., 17, 21) | String | No |
#   | `xcode-version`| Xcode version to use (e.g., 16.4) | String | No       |
#
### Usage Example
#   ```yaml
# name: PR Checks
# Trigger conditions for the workflow
#  on:
#    push:
#      branches: [ dev ]  # Runs on pushes to dev branch
#    pull_request:       # Runs on all pull requests
#
#  # Concurrency settings to prevent multiple simultaneous workflow runs
#  concurrency:
#    group: pr-${{ github.ref }}
#    cancel-in-progress: true  # Cancels previous runs if a new one is triggered
#
#  permissions:
#    contents: write
#
#  jobs:
#    pr_checks:
#      name: PR Checks
#      uses: openMF/mifos-x-actionhub/.github/workflows/pr-check.yaml@v1.0.6
#      with:
#        android_package_name: 'cmp-android' # <-- Change Your Android Package Name
#        desktop_package_name: 'cmp-desktop' # <-- Change Your Desktop Package Name
#        web_package_name: 'cmp-web' # <-- Change Your Web Package Name
#        ios_package_name: 'cmp-ios' # <-- Change Your iOS Package Name
#        build_ios: true # <-- Change to 'false' if you don't want to build iOS
#        use_cocoapods: true # <-- Required if build_ios = true
#        shared_module: ':cmp-shared' # <-- Required if build_ios = true
#        java-version: '21' # <-- Set according to the JDK your project uses (default is 17)
#   ```
#
# GitHub Actions workflow for continuous integration of Mobile-Wallet project
# Runs on master and dev branches to ensure code quality and build stability
name: Debug Build & PR Checks

# Trigger conditions for the workflow
on:
  workflow_call:
    inputs:
      java-version:
        description: 'Java version to use (e.g., 17, 21)'
        type: string
        required: false
        default: '17'

      xcode-version:
        description: 'Xcode version to use (e.g., 16.4).'
        required: false
        default: '16.4'
        type: string

      android_package_name:
        description: 'Name of the Android project module'
        type: string
        required: true

      desktop_package_name:
        description: 'Name of the Desktop project module'
        type: string
        required: true

      web_package_name:
        description: 'Name of the Web project module'
        type: string
        required: true

      ios_package_name:
        description: 'Name of the iOS project module'
        type: string
        required: true
        
      build_ios:
        description: 'Build iOS Application'
        type: boolean
        required: false  
        default: true

      use_cocoapods:
        description: >
          Indicates whether to use CocoaPods integration for the iOS build.
          Set to `true` if your project integrates the Kotlin Multiplatform shared module
          into the iOS app using CocoaPods (e.g., via a `.podspec` file and `pod install`).
        type: boolean
        required: true

      shared_module:
        description: >
          The Gradle path to the Kotlin Multiplatform shared module used in the iOS target.
          This is the module that will be compiled as a framework and optionally exposed
          to the iOS project via CocoaPods (e.g., `:cmp-shared`).
          It is used when generating a dummy framework for pod install to succeed.
        type: string
        required: true

      compose_resources_dir:
        description: 'Path to Compose resources directory'
        type: string
        default: 'cmp-shared/build/compose/cocoapods/compose-resources'

# Concurrency settings to prevent multiple simultaneous workflow runs
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true  # Cancels previous runs if a new one is triggered

jobs:
  checks:
    name: Static Analysis Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Static Analysis Check
        uses: openMF/mifos-x-actionhub-static-analysis-check@v1.0.1
        with:
          java-version: ${{ inputs.java-version }}

  build_android_app:
    name: Build Android Application
    needs: [ checks ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build Android App
        uses: openMF/mifos-x-actionhub-build-android-app@v1.0.2
        with:
          android_package_name: ${{ inputs.android_package_name }}
          java-version: ${{ inputs.java-version }}

  build_desktop_app:
    name: Build Desktop Application
    needs: [ checks ]
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Desktop App
        uses: openMF/mifos-x-actionhub-build-desktop-app-kmp@v1.0.1
        with:
          desktop_package_name: ${{ inputs.desktop_package_name }}
          build_type: 'Debug'
          java-version: ${{ inputs.java-version }}

  build_web_app:
    name: Build Web Application
    needs: [ checks ]
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Web App
        uses: openMF/mifos-x-actionhub-build-web-app-kmp@v1.0.1
        with:
          web_package_name: ${{ inputs.web_package_name }}
          java-version: ${{ inputs.java-version }}

  build_ios_app:
    name: Build iOS App
    if: ${{ inputs.build_ios }}
    needs: [ checks ]
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup latest Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ inputs.xcode-version }}

      - name: Install CocoaPods
        if: ${{ inputs.use_cocoapods }}
        run: sudo gem install cocoapods

      - name: Prepare compose resources directory
        run: mkdir -p ${{ inputs.compose_resources_dir }}

      - name: Generate dummy framework for KMP
        if: ${{ inputs.use_cocoapods }}
        run: ./gradlew ${{ inputs.shared_module }}:generateDummyFramework

      - name: Build iOS App
        if: ${{ inputs.build_ios }}
        uses: openMF/mifos-x-actionhub-build-ios-app@v1.0.2